{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "User configuration for Follow Builders skill. Stored at ~/.follow-builders/config.json",
  "type": "object",
  "properties": {
    "language": {
      "type": "string",
      "enum": ["en"],
      "default": "en",
      "description": "Digest language: en (English)"
    },
  }
}
